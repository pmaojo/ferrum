# OAuth Demo

Minimal project using the `auth-oauth` plugin.

```bash
# install ferrum
cargo install --path ../..

# add plugin and compile
ferrum add auth-oauth
ferrum compile gen/app.yaml
ferrum dev
```
