[package]
name = "ferrum-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
neo4rs = { version = "0.9.0-rc.6", features = ["json"] }
ferrum-shared-models = { path = "../shared-models" }
async-graphql = "7"
lettre = { version = "0.11", features = ["builder", "smtp-transport"] }
