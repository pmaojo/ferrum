[package]
name = "ferrus-plugin-template"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
ferrum-engine = { path = "../engine" }
ferrum-shared-models = { path = "../shared-models" }
anyhow = "1"
