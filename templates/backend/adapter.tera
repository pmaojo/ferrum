use async_trait::async_trait;
use sqlx::PgPool;

use crate::ports::{{ node.implements }};
use crate::shared_models::{{ module_name }}::{{ module_name | capitalize }};

pub struct {{ node.id | capitalize }} {
    pool: PgPool,
}

impl {{ node.id | capitalize }} {
    pub fn new(pool: PgPool) -> Self {
        Self { pool }
    }
}

#[async_trait]
impl {{ node.implements | capitalize }} for {{ node.id | capitalize }} {
    async fn get_{{ module_name | lower }}(&self, id: uuid::Uuid) -> {{ module_name | capitalize }} {
        // In a real implementation, this would query the database
        // For now, we return a placeholder
        {{ module_name | capitalize }} {
            id,
            name: "Example".to_string(),
            created_at: chrono::Utc::now(),
        }
    }
}
