use async_trait::async_trait;
use uuid::Uuid;

use crate::shared_models::{{ module_name }}::{{ module_name | capitalize }};

#[async_trait]
pub trait {{ node.id | capitalize }} {
    async fn get_{{ module_name | lower }}(&self, id: Uuid) -> {{ module_name | capitalize }};
}
