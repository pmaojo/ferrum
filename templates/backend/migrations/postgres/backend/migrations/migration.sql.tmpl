{% set module = modules[0] %}
{% set entity = module.entities_info[0] %}
-- Auto-generated migration for {{ entity.name }}
CREATE TABLE IF NOT EXISTS {{ entity.table }} (
    id UUID PRIMARY KEY,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL
    {% for field in entity.fields %}
    , {{ field.name_snake }} {% if field.type == "uuid" %}UUID{% elif field.type == "string" %}VARCHAR(255){% elif field.type == "text" %}TEXT{% elif field.type == "boolean" %}BOOLEAN{% elif field.type == "timestamp" %}TIMESTAMP WITH TIME ZONE{% elif field.type == "date" %}DATE{% elif field.type == "decimal" %}DECIMAL(10,2){% elif field.type == "integer" %}INTEGER{% else %}TEXT{% endif %}
    {% endfor %}
);
