# Auth Password Plugin

This plugin adds basic password-based authentication scaffolding.

## Usage

```bash
ferrum add auth-password
ferrum compile gen/app.yaml
```

The plugin copies example login handlers and frontend components to your project and ensures required environment variables exist.
