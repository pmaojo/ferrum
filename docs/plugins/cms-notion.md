# CMS Notion Plugin

This plugin allows your app to fetch structured content from Notion databases via the official API.
It creates a `NotionClient` resource and ensures a `NOTION_API_TOKEN` variable in `.env`.

## Usage

```bash
ferrum add cms-notion
ferrum compile gen/app.yaml
```
